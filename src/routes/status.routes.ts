import { Router } from "express";

const router = Router();

// Status polling is handled by POST /api/attendance/heartbeat
// This route file is kept as a placeholder for future status-related endpoints.

export default router;