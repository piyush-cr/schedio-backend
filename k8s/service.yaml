apiVersion: v1
kind: Service
metadata:
  name: schedio-api
  namespace: schedio
  labels:
    app: schedio
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: schedio
    component: api
