  # Optional: expose API externally. Uncomment and adjust for your ingress controller.
  # For nginx-ingress, cert-manager, etc.
  # apiVersion: networking.k8s.io/v1
  # kind: Ingress
  # metadata:
  #   name: schedio-api
  #   namespace: schedio
  #   annotations:
  #     kubernetes.io/ingress.class: nginx
  #     cert-manager.io/cluster-issuer: letsencrypt-prod
  # spec:
  #   tls:
  #     - hosts:
  #         - api.yourdomain.com
  #       secretName: schedio-tls
  #   rules:
  #     - host: api.yourdomain.com
  #       http:
  #         paths:
  #           - path: /
  #             pathType: Prefix
  #             backend:
  #               service:
  #                 name: schedio-api
  #                 port:
  #                   number: 3000
